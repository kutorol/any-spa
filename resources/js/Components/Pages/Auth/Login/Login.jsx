import React from 'react'
import AuthFormWrapper from "../Common/AuthFormWrapper";

const Login = () => {
  return (
    <AuthFormWrapper
      isRegister={false}
    />
  );
};

export default Login;
