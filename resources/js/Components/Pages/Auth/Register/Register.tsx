// @ts-ignore
import React from "react";
import AuthFormWrapper from "../Common/AuthFormWrapper";

const Register = () => {
  return (
    <AuthFormWrapper
      isRegister={true}
    />
  );
};

export default Register;
